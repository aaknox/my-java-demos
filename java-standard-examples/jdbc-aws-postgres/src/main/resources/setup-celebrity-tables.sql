CREATE TABLE CELEBRITY(
C_ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
C_FIRST_NAME CHARACTER VARYING(40),
C_LAST_NAME CHARACTER VARYING(20),
C_STAGE_NAME CHARACTER VARYING(40),
C_GENDER CHARACTER VARYING(20),
PRIMARY KEY (C_ID)
);

INSERT INTO CELEBRITY (C_FIRST_NAME, C_LAST_NAME, C_STAGE_NAME, C_GENDER)
VALUES('Jim','Carrey',NULL,'M');

SELECT * FROM CELEBRITY; 